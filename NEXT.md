# NEXT

Write exactly one next action here at the end of every session.
The next session starts by reading it. No lists.

---

Session 1: `pip install -r requirements.txt`, then `quarto preview` and check
the test post renders with figures and audio. Then paste
`ghost/code-injection.html` into Ghost and build the test post from
`milestones/00-test-post/ghost-post.md`.
